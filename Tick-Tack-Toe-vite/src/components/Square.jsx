const Square = (props) =>{
    // prop is OBJECT that is passed to the component
    console.log(prop);
    // prop.value would print string 5
    console.log(prop.value);
    return (
        <p>Header Section goes here</p>
    )
}

export default Square;